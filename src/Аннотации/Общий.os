// Инициализирует класс как одиночный объект, одиночные объекты создаются один раз для всего приложения,
// так же экземпляр автоматически импортируется
//
// Пример:
//
// &Общий
// Перем Этот;
//
&Аннотация("Импорт")
Процедура ПриСозданииОбъекта()

КонецПроцедуры